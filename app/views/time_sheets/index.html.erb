<table>
<% @time_sheets.each do |time_sheet| %>
	<tr>
		<td><%= time_sheet.user.name %></td>
		<td><%= time_sheet.start_date %></td>
	</tr>
<% end %>
</table>

<%= link_to "New Timesheet", new_time_sheet_path %>